import { ethers } from 'ethers';
export type CtHandleContractPair = {
    ctHandle: bigint;
    contractAddress: string;
};
export declare const userDecryptRequest: (kmsSignatures: string[], gatewayChainId: number, chainId: number, verifyingContractAddress: string, aclContractAddress: string, relayerUrl: string, provider: ethers.JsonRpcProvider | ethers.BrowserProvider) => (handles: CtHandleContractPair[], privateKey: string, publicKey: string, signature: string, contractAddresses: string[], userAddress: string, startTimestamp: string | number, durationDays: string | number) => Promise<bigint[]>;
