import { BrowserProvider, Eip1193Provider, JsonRpcProvider, Provider } from 'ethers';
import { PublicParams } from './encrypt';
import { TfheCompactPublicKey } from 'node-tfhe';
export type HTTPZInstanceConfig = {
    verifyingContractAddress: string;
    kmsContractAddress: string;
    aclContractAddress: string;
    gatewayChainId: number;
    chainId?: number;
    relayerUrl?: string;
    network?: Eip1193Provider | string;
    publicParams?: PublicParams<Uint8Array> | null;
    publicKey?: {
        data: Uint8Array | null;
        id: string | null;
    };
};
export declare const getProvider: (config: HTTPZInstanceConfig) => JsonRpcProvider | BrowserProvider;
export declare const getChainId: (provider: Provider, config: HTTPZInstanceConfig) => Promise<number>;
export declare const getTfheCompactPublicKey: (config: HTTPZInstanceConfig) => Promise<{
    publicKey: TfheCompactPublicKey;
    publicKeyId: string;
}>;
export declare const getPublicParams: (config: HTTPZInstanceConfig) => Promise<PublicParams>;
export declare const getKMSSigners: (provider: Provider, config: HTTPZInstanceConfig) => Promise<string[]>;
